{
  "name": "memoir",
  "version": "1.2.1",
  "description": "",
  "main": "./dist/index.js",
  "scripts": {
    "clean": "rm -rf ./dist/*",
    "build": "tsc --build .",
    "clean:test": "rm -rf ./test/dist/*",
    "build:test": "tsc --build ./test",
    "test": "npm run clean && npm run build && npm run clean:test && npm run build:test && node ./test"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/faranalytics/memoir.git"
  },
  "author": "FAR",
  "license": "MIT",
  "bugs": {
    "url": "https://github.com/faranalytics/memoir/issues"
  },
  "homepage": "https://github.com/faranalytics/memoir#readme",
  "devDependencies": {
    "typescript": "^5.1.3",
    "@types/node": "^20.3.1"
  },
  "dependencies": {}
}
